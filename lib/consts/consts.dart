import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

const Color COLOR_BACKGROUND_DARKER = Color.fromRGBO(49, 62, 80, 1.0);
const Color COLOR_BACKGROUND = Color.fromRGBO(71, 82, 98, 1.0);
const Color COLOR_INPUT_FIELDS = Color.fromRGBO(184, 225, 255, 0.5);

const Radius BORDER_RADIUS = Radius.circular(25);
const Radius BORDER_RADIUS_INPUTS = Radius.circular(13);